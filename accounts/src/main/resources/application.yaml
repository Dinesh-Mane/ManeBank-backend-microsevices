spring:
  application:
    name: ${APP_NAME:accounts}

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:accounts}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}